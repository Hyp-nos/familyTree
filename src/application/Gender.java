package application;


public enum Gender 
	
	{ MALE, FEMALE;

 // for debugging, Delete later
	public static String tooString(Gender g){
		if (g==Gender.MALE)
	return "Male";
		else return "Female";
}
}